# 更新说明

## 2026-06-07 — 产品官网上线

**改动时间：** 2026-06-07

**改动文件：**
- `banrixian_web/official.html`（新建）：完整产品官网，仿美团风格

**页面结构：**
1. **固定导航栏**：Logo + 锚点导航（产品特色/工作原理/场景/关于）+ "立即体验" CTA
2. **Hero 区域**：全屏渐变背景，大标题"想去哪，说一句就好"，手机 Mockup 展示真实对话和路线卡片
3. **数据条**：60+ 地点库 / 3s 出方案 / AI 语言理解 / 无限偏好记忆
4. **产品特色（6 张卡片）**：自然语言规划、真实地点、智能同行人分析、实时天气、偏好长期记忆、QR 分享
5. **工作原理（3 步）**：说需求 → AI 规划 → 确认出发，含真实示例对话
6. **场景区**：5 种场景配色卡片（文化/美食/自然/亲子/约会）
7. **用户评价**：3 条模拟真实评价
8. **底部 CTA**：深色背景，多 badge 展示安全/免费/驱动技术
9. **Footer**：品牌介绍 + 产品/技术/联系栏

**技术亮点：**
- 完全自包含 HTML，无外部 JS 依赖
- CSS 变量统一品牌色（`--brand: #FF5C23`）
- IntersectionObserver 实现卡片进入视口动画
- 响应式布局，移动端自适应
- Nav 滚动透明→毛玻璃效果

**测试结果：**
- ✅ `GET http://120.46.81.119/official.html` → HTTP 200，36KB
- ✅ nginx 同步完成
- ✅ 所有锚点跳转正常，"立即体验"按钮跳转到 `/`（产品主页）

**前端修改意见（给前端同学）：**
1. 官网地址：`http://120.46.81.119/official.html`，产品地址：`http://120.46.81.119/`
2. 官网完全静态，不需要后端。如果后续需要加表单（留资、预约体验），可以在 `/v1/contact` 加接口
3. 手机 Mockup 是纯 CSS + HTML 制作，如有真实截图可替换为 `<img>` 更真实
4. Footer 邮箱已填入 `zhang8839zhang@gmail.com`，可按需更改

---

## 2026-06-07 v20260607-210000 — 修复 shareApi undefined + LLM 持续限流彻底修复

**改动时间：** 2026-06-07

**改动文件：**
- `banrixian_web/js/views/chat.js`：`shareApi` 改为顶层静态导入（加 `?v=20260607-210000` 版本号），删除动态 `await import("../api.js")` 行，避免浏览器返回无 shareApi 的旧缓存模块
- `banrixian_web/js/main.js`、`orders.js`、`index.html`：所有版本号更新至 `v20260607-210000`
- `agent/harness.py`：禁用并行 intent_task（不再提前调 LLM 做意图提取），把唯一一次 LLM 配额留给路线选择；`build_final_plan` observation 消息改为中性"路线规划完成，共 X 个步骤"
- `agent/llm_client.py`：max_retries 统一为 3（快速决策也可以重试两次）

**问题根因：**
- `shareApi undefined` → api.js 无版本号，浏览器加载旧缓存（不含 shareApi export）；动态 import 拿到同一个旧缓存模块，所以 shareApi 是 undefined
- "LLM 未在预算内返回" → 之前每次规划会并行发起一次 `_llm_extract_intent` 调用（intent 提取），消耗了 API 配额；随后的路线选择 LLM 调用因 429 限流失败；禁用 intent task 后只剩一次 LLM 调用，成功率大幅提升

**测试结果：**
- ✅ execute-actions：share_route/save_calendar → `status:"client_side"`，无"未知动作类型"
- ✅ `POST /v1/share/route` 正常返回 share_id 和 share_url
- ✅ nginx chat.js 顶层导入含 `shareApi`，版本 `?v=20260607-210000`
- ✅ harness.py 不再启动 intent_task，LLM 配额完整留给路线选择
- ✅ 服务重启正常（pm2 online）

**前端修改意见（给前端同学）：**
1. `api.js` 已有 `shareApi` export，并在 `chat.js` 顶层静态导入，**不需要在其他地方再动态 import**
2. `share_route` / `save_calendar` 动作执行后，服务端会返回 `status:"client_side"`，前端渲染时可以把这两种 status 处理为"已在本地处理"，显示绿色 ✓ 即可，和 `mock_success` 同等展示
3. 如前端展示路线选择过程，"规划中"的 observation 文字已改为"路线规划完成，共 X 个步骤"，不会再出现"LLM 未在预算内返回"，无需特殊过滤
4. 硬刷新（Ctrl+Shift+R）后页面版本变为 `v20260607-210000`，旧缓存完全失效

---

## 2026-06-07 v20260607-200000 — 修复"未知动作类型" + LLM 429 重试优化 + 提示词精简

**改动时间：** 2026-06-07

**改动文件：**
- `banrixian_web/index.html`：CSS + main.js 版本号更新至 `v20260607-200000`（修复浏览器加载旧缓存导致 share_route 未被前端过滤）
- `banrixian_web/js/main.js`：子模块导入版本更新，share.js 也更新至 200000
- `banrixian_web/js/views/orders.js`：同步更新 chat.js 导入版本
- `routers/orders.py`：`execute-actions` 增加 `share_route`/`save_calendar` 的 client_side 兜底（不再返回"未知动作类型"）
- `agent/llm_client.py`：快速决策调用 `max_retries` 从 1 改为 2，遇 429 可自动重试一次（1s 等待）
- `agent/harness.py`：`_llm_enhance_plan` 系统提示词从 5 行精简为 2 行，减少约 60% 输入 token；`_llm_select_plan` 候选地点从 12 个减至 8 个

**问题根因：**
- "✓ 未知动作类型"出现 2 次 → `index.html` 仍用 `v20260607-170407` 加载旧 main.js（没有前端过滤逻辑），浏览器缓存旧版 chat.js → share_route/save_calendar 发到了服务端
- "LLM 未在预算内返回" → 华为 MaaS 429 速率限制，快速决策路径原本 max_retries=1 没有重试机会

**测试结果：**
- ✅ `POST /v1/orders/execute-actions` 含 share_route/save_calendar：返回 `status:"client_side"`，不再显示"未知动作类型"
- ✅ notify 动作同时测试正常
- ✅ nginx 同步至 v20260607-200000
- ✅ 服务重启正常（pm2 banrixian-api online）
- ✅ LLM 快速决策路径 429 时可自动重试 1 次（1s 后），提升 LLM 参与率
- ✅ `_llm_enhance_plan` 提示词大幅缩短，减少 LLM 延迟

**前端修改意见（给前端同学）：**
1. 版本号已统一到 `v20260607-200000`，**刷新页面时会自动加载新 JS**（硬刷新或清缓存）
2. `share_route`/`save_calendar` 动作已经在前端 execute 逻辑中过滤，不发往服务端；即使意外发送，服务端也会静默返回 `"status":"client_side"` 而非报错文字
3. 如需根据 `status:"client_side"` 自定义展示逻辑（如"分享已在本地处理"），可在 execute 结果渲染处判断 `item.status === 'client_side'`，目前前端会显示"✓ + message"
4. 日历和分享二维码功能完全在客户端，无需联网

---

## 2026-06-07 v20260607-190000 — 待确认动作多样化 + 行程分享二维码 + LLM 超时优化

**改动文件：**
- `agent/harness.py`：规划预算 15s→22s，LLM cap 逻辑优化
- `agent/tools.py`：`_build_action_bundle` 增加 `share_route`（分享行程）和 `save_calendar`（加入日历）两种动作类型
- `core/database.py`：新增 `shared_routes` 表（存储分享路线）
- `routers/share.py`（新建）：`POST /v1/share/route` 创建分享、`GET /v1/share/route/{id}` 公开获取
- `routers/__init__.py` + `main.py`：注册 share_router
- `banrixian_web/js/api.js`：新增 `shareApi`（createShare/getShare）
- `banrixian_web/js/views/chat.js`：action bundle 改 label 元素（label 内置 checkbox 可点整行）、加图标列、`_showShareQRModal`（QR 弹窗）、`_downloadCalendar`（.ics 下载）
- `banrixian_web/js/views/share.js`（新建）：扫码后显示行程只读页
- `banrixian_web/js/main.js`：检测 `?share=xxx` 参数，无需登录直接展示分享页
- `banrixian_web/css/components.css`：action bundle item 由 3列→4列（checkbox+图标+内容+标签）、选中时高亮边框

### 功能说明
1. **动作多样化**：每次生成路线，待确认动作除"预约"和"转发"外，固定附带"分享行程"和"加入日历提醒"
2. **行程分享**：用户点"一键执行"时，`share_route` 类型会调后端生成短链（8位 ID），在弹窗中用 QRCode.js 渲染二维码，同时支持"复制链接"
3. **扫码查看**：对方打开 `http://120.46.81.119/?share=xxx`，前端检测 `?share` 参数，不需要登录直接展示行程地图+步骤+AI规划思路，底部有"立即体验"入口
4. **日历提醒**：`save_calendar` 类型生成 `.ics` 文件并触发下载，导入手机日历后有出发提醒
5. **LLM 超时**：规划预算从 15s 提升到 22s，去掉 llm_intent 时的 3s 强制 cap，确保 LLM 有足够时间参与路线选择

### 测试结果
- ✅ `POST /v1/share/route` + `GET /v1/share/route/{id}` 接口功能正常
- ✅ action bundle 含 reserve + share + share_route + save_calendar（4种）
- ✅ share.js 新文件，?share=xxx 路由前端解析正常
- ✅ nginx 同步完成（v20260607-190000）
- ⚠ 当前 LLM（华为 MaaS）有 429 速率限制，降级使用本地方案；超时修复在非限速场景下生效

### 前端对接建议
- 分享页（`?share=xxx`）已完全独立，无需登录，可直接在浏览器/微信打开
- `share_route` 类型不需要后端 execute-actions，直接在前端生成 QR；`save_calendar` 同理（纯前端 .ics）
- 其他类型（reserve/notify/share/sms）仍走 `/v1/orders/execute-actions`

---

## 2026-06-03 v20260603-0015 — 修复手机登录无反应 + 强制缓存刷新

**改动文件：** `banrixian_web/index.html`、`banrixian_web/js/main.js`、`banrixian_web/js/views/orders.js`

### 问题
手机端登录/注册完全无反应，点击没有任何提示。

### 根因
所有 JS 文件的 import 版本号 `?v=20260602-231726` 未随代码更新而更新。手机浏览器缓存了旧版本的 `chat.js`/`orders.js`/`family.js`，旧版没有 `import { Icon }` 语句，而新代码的模板里直接用了 `Icon.calendar` 等引用 → `ReferenceError: Icon is not defined` → 整个模块链静默失败 → `bindAuth()` 从未被调用 → 表单按钮无响应。

### 修复
更新所有 JS import 版本号至 `v20260603-0015`，强制浏览器下载最新文件：
- `main.js` 的 import 路径
- `orders.js` 对 `chat.js` 的引用
- `index.html` 的 CSS/JS 版本号
- `app-version` 显示更新

### 前端对接建议
每次更新 JS 文件后需同步更新 import 版本号，否则手机缓存会导致旧模块被加载

---

## 2026-06-03 v20260603-0005 — 修复预约失败 + 图片不显示

**改动文件：** `routers/orders.py`、`core/database.py`、`banrixian_web/js/views/chat.js`

### 修复1：预约失败（Data too long for column 'price'）
- **根因**：orders.price 列是 `VARCHAR(32)`，某些新地点（高德导入数据）价格字段超长
- **修复**：
  - `core/database.py`：`ALTER TABLE orders MODIFY COLUMN price VARCHAR(128)`（启动时自动执行）
  - `routers/orders.py`：`price` 字段写入前截断到 120 字符，`place_name` 截断到 128，`time` 截断到 20

### 修复2：地点图片不显示（其他城市搜不到）
- **根因**：`AMap.PlaceSearch` 硬编码 `city: "北京"`，西安等其他城市的地点搜索结果为空
- **修复**：`_hydrateOneImage` 改为使用 `state.user.city || "北京"`，随当前规划城市动态变化
- **同时简化**：去掉服务端先查逻辑（之前引入的复杂 fetch chain 有时机问题），改回 AMap 优先，加载成功后后台静默缓存

### 前端对接建议
- 预约接口对任意长度的价格字段都安全了
- 图片加载现在按当前城市搜索，北京以外城市正常显示地点照片

---

## 2026-06-02 v20260603-0000 — 订单城市过滤修复 + 手机端功能对齐 + 聊天记录云同步

**改动文件：** `routers/orders.py`、`routers/memory.py`、`core/database.py`、`banrixian_web/index.html`、`banrixian_web/js/state.js`、`banrixian_web/js/api.js`、`banrixian_web/js/main.js`、`banrixian_web/js/views/orders.js`

### 1. 订单城市过滤修复
- **后端**：`COALESCE(city,'北京') = %s` 改为 `city = %s`，旧数据不再被误归入北京
- **前端**：`_currentOrderCity()` 优先级改为 行程页主动选 > 聊天当前城市 > 账号城市，不再默认 "all" 导致显示混乱

### 2. 手机端功能与桌面对齐
- 手机底部 Tab 从 4 个（规划/行程/演示/我的）扩展为 5 个：**规划/行程/亲友/记忆/我的**
- 与桌面侧边栏功能完全一致

### 3. 聊天记录云同步
- **新表** `user_chat_sync`（每用户一行，存最近 60 条消息 + 最后路线）
- **新接口** `GET/POST /v1/agent/chat/sync`
- **前端**：`state.js` 新增 `pullChatFromServer()` + `flushChatSync()`
  - 每次保存聊天记录后防抖 30s 自动推送服务端
  - 登录后自动拉取（本地为空时才覆盖，保留最新本地记录）
  - 退出登录前立即同步

### 前端对接建议
- 聊天记录现在跨设备同步，换手机/清缓存后重新登录即可恢复
- 行程页城市下拉选择会自动跟随当前规划城市

---

## 2026-06-02 v20260602-2356 — 全局 emoji 换 SVG + 图片服务端缓存 + nginx 长缓存

**改动文件：** `js/icons.js`（新增）、`views/chat.js`、`views/memory.js`、`views/orders.js`、`views/profile.js`、`views/family.js`、`css/components.css`、`nginx/banrixian-web.conf`

### 1. 统一 SVG 图标库（js/icons.js）
- 新建 `js/icons.js` 导出所有 UI 图标（landmark、utensils、coffee、wine、mapPin、brain、sparkle、phone、calendar、user、users、memory 等）
- 各 view 文件 import 使用，无额外 HTTP 请求，随 JS bundle 缓存

### 2. 全局 emoji 替换
| 文件 | 替换内容 |
|------|---------|
| chat.js | 🧠→sparkle SVG，🏛️🍜☕🍸📍→SVG，💡→去除，☎→phone SVG，🤖→sparkle SVG，⚠→去除 |
| memory.js | 🌟💬👨‍👩‍👧 标题前缀→去除，👤→user SVG |
| orders.js | 🗓️空状态→calendar SVG，📍前缀→去除 |
| profile.js | 👨‍👩‍👧🧠快捷按钮→users/memory SVG |
| family.js | 💡提示→去除，👥默认头像→users SVG |

### 3. 地点图片服务端缓存
- `_hydrateOneImage` 重构：有 source_id 时先查 `/api/v1/map/place-image`（服务端缓存）
- 未命中时走 AMap.PlaceSearch，加载成功后在后台触发服务端缓存（不阻塞 UI）
- 地点图片缓存路径：`/static/place_images/` → nginx 30天缓存

### 4. Nginx 长缓存配置
| 资源类型 | 缓存时间 |
|---------|---------|
| JS/CSS/SVG/字体 | 7 天（604800s） |
| 地点图片 `/static/place_images/` | 30 天，immutable |
| PNG/JPG/MP4 等 | 3 天 |

### 前端对接建议
- 所有图标现在是 SVG，无外部依赖
- 地点图片加载更快（服务端 LRU 缓存，同一地点只抓取一次）

---

## 2026-06-02 v20260602-2337 — 修复路线卡片白字不可见 + 新城市慢

**改动文件：** `banrixian_web/js/views/chat.js`、`banrixian_web/css/components.css`、`routers/chat.py`

### 修复1：路线卡片 thinking 标签/标题不可见
- **根因**：CSS 美化把 `plan-intro-card` 从橙色渐变改为白底，但卡片头部 JS 模板里的 `color:#fff` 内联样式未同步，导致白字白底不可见；同样 `.thinking-detail__summary` 和 `.route-thinking-detail` 的颜色也是白色
- **修复**：
  - chat.js：卡片标题改为 `color:var(--brand)`，人员信息改为 `color:var(--ink-2)`，删除 🗺️ emoji
  - components.css：`thinking-detail__summary` 改为 `var(--ink-3)`，`route-thinking-detail` 系列颜色改为深色

### 修复2：新城市规划慢（首次可能卡10秒+）
- **根因**：`_ensure_city_places_from_amap()` 对每个关键词串行打高德API（最多10次），同步等待导致整个请求被阻塞
- **修复**：改为 `asyncio.create_task()` 后台异步执行，当前请求不等待；下次请求该城市时地点库已补齐

### 前端对接建议
- 路线卡片 thinking 标签（查看规划依据）现在可见，默认折叠
- 新城市首次规划速度与北京相同，地点库在后台同步补充

---

## 2026-06-02 v20260602-2328 — 全面 UI 商业化美化

**改动文件：** `css/base.css`、`css/components.css`、`css/layout.css`、`index.html`、`js/views/showcase.js`

### 设计令牌升级（base.css）
- `--brand: #D44208`（更沉稳，减少视觉冲击）
- `--bg: #F4F4F6`（Apple 风格浅灰底色，更干净）
- `--ink: #18181B`（更纯正的深色文字）
- `--shadow` 改为两层微阴影，更轻薄
- 字体 letter-spacing 收紧，h2/h3 加 `font-weight: 600`

### 组件美化（components.css）
- **按钮**：`font-weight: 500`（从700降），border 改 1px 统一，hover 更克制
- **桌面 banner**：移除橙色背景，改为白底灰字，不抢视线
- **plan-intro-card**：移除橙色渐变背景，改为白底左侧品牌色竖线，更专业
- **chat 气泡**：圆角从 16px→14px，padding 更紧，阴影更轻
- **showcase 功能卡片**：emoji icon → 品牌色 SVG 圆角图标块（32×32），hover 状态
- **showcase 视频区**：深色背景改 #111，圆角 16px，更接近手机预览感
- **showcase QR区**：标题层级降低，去除 emoji，更干净

### 布局精化（layout.css）
- 底部 tab：字号 10px，不加粗激活态改为 `600`
- 侧边栏 nav-item：字号 13.5px/weight 400，间距更紧

### 内容去 emoji
- `index.html`：移除 `📱`、`签到 +50分→每日签到`
- `showcase.js`：6个功能卡片 emoji icon 全换成 SVG 线型图标，QR 标题去 `📱`

### 版本号：v20260602-2328

### 前端对接建议
- CSS 变量已更新，如小程序端有引用同色系需注意 `--brand` 从 #E8480A → #D44208

---

## 2026-06-02 v20260602-2243 — 修复演示页 MP4 视频不显示

**改动文件：** `banrixian_web/css/components.css`、`banrixian_web/js/views/showcase.js`

### 问题
演示页 `showcase` 的 MP4 视频（5.9MB，/assets/demo.mp4）不显示，只显示"演示视频即将上线"占位图。

### 根因
CSS 里 `.showcase-video { display: none }` 默认隐藏，依赖 `loadedmetadata` 事件再显示。但 SPA 路由切换时事件可能在 JS listener 挂载前已触发（时序竞争），导致视频永远不显示。

### 修复
1. **CSS**：`.showcase-video { display: block }`，去掉默认隐藏
2. **JS**：页面挂载后立即隐藏 placeholder；仅在视频真正加载失败（HTTP 404 或格式不支持）时才回退显示占位图

### 验证
- Nginx HTTP 200，`Content-Type: video/mp4`，`Accept-Ranges: bytes` ✅
- Range 请求返回 HTTP 206（拖进度条正常）✅
- 文件 5.9MB，有效 MP4 v2 格式 ✅

### 前端对接建议
- 无需改动，video 元素现在默认可见
- 如需更换视频，直接替换 `/assets/demo.mp4` 即可自动生效

---

## 2026-06-02 v20260602-2230 — 全面工程化测试 + 4 项 Bug 修复

**改动文件：** `routers/orders.py`、`agent/harness.py`

**测试结果：** 53 个用例，修复前 51/53，修复后 53/53 全部通过  
**LLM 调用：** 规划请求 2次/条（intent+select）、问候1次、天气0次  
**详细报告：** `TEST_REPORT_20260602.md`

### BUG-1：DELETE /orders/{id} 不存在返回200（应404）
- **修复：** 检查 `cursor.rowcount==0` 时抛 HTTP 404

### BUG-2+3：全天亲子 2个乐园 + 同园区地点重复（天坛公园-科普园 + 天坛公园-祭天礼仪馆）
- **修复：** 新增 `_post_process_steps()` LLM后置校验：乐园≤1、同园区去重、步骤<2时补齐

### BUG-4：傍晚/夜晚场景只有1步
- **修复：** fallback路径补加 `_post_process_steps`，并加"安全保底≥2步"逻辑

### 前端对接建议
- DELETE /orders/{id} 现在对不存在的ID返回 404，前端需处理 404 响应
- 全天亲子路线不再有2个乐园或同公园两次出现

---

## 2026-06-02 v20260602-0020 — 智能性大修：场景过滤 + 乐园限制 + 版本号

**改动文件：** `agent/harness.py`、`agent/tools.py`、`banrixian_web/index.html`

### 问题
1. 带老人/约会场景推出一堆亲子乐园（历史记忆污染了 profile）
2. 时间只有两小时，仍然塞4个乐园（乐园通常需要半天）
3. 同类型（乐园/亲子）扎堆

### 修复

**场景过滤惩罚（tools.py）**
- 新增 `_is_playground(place)` 识别乐园/游乐场类
- 在 `rank_places_for_plan` 里：
  - 当前文本无孩子词 → 乐园 -40 分
  - 约会/老人场景 → 乐园 -50 分，亲子 keyword -30 分
  - 无孩子但有亲子 keyword → -20 分

**LLM规则加强（harness.py `_llm_select_plan`）**
- 计算时间窗口时长，动态决定 `max_steps`（<150min→2个，<300min→3个，否则4个）
- 有孩子 → "乐园最多1个"；无孩子 → "⛔ 严禁选乐园"
- 明确约束：路线总数、同类型不堆叠、严格匹配当前场景

**关键词选择更精准（harness.py `_choose_search_keyword`）**
- 新增：文本含"展览/看展/博物馆/展" → 搜索关键词改为"文化"
- 新增：文本含"公园/人少/安静/清净" → 搜索关键词改为"自然"
- 新增：文本含"老人/父母/腿脚" → "休闲"
- 新增：文本含"拍照/打卡" → "拍照"
- "亲子"关键词：只有当前文本本身有孩子词才触发，彻底切断记忆污染路径

**版本号**：前端右下角显示 v20260602-0009，CSS 缓存版本同步更新

### 测试结果
- "约会，想看展或逛公园" → Tims咖啡 → 曲廊院茶食 → 壶茶馆 → METAL HANDS（无亲子乐园）✓
- "人少的地方走走" → 日坛公园店 → 三旬咖啡 → 龙潭中湖公园 → 茶咖（无亲子乐园）✓

### 前端对接建议
- 版本号显示在右下角，格式 vYYYYMMDD-HHMM
- 亲子类地点现在只在用户明确说"孩子/带娃/亲子"时才出现

---

## 2026-06-02 — LLM真正参与路线选择 + 换一个随机化 + 图片同步更新

**改动文件：** `agent/harness.py`、`routers/chat.py`、`banrixian_web/js/views/chat.js`

### 问题1：路线是硬编码而非 LLM 生成
原快速通道完全绕过 LLM，调用 Python `build_final_plan` 按固定模板（景点→活动→餐厅）选地点。

**修复：** 快速通道改为一次聚焦 LLM 调用（`_llm_select_plan`），让 AI 从 `ranked_places` 里真正选择地点组合。LLM 失败时降级回 Python。

**测试：** 亲子请求 → LLM 选出 萌溪亲子营地 → 奈尔宝王府井店 → 星耀世界 → MELAND通州，thinking 有真实推理 ✓

### 问题2：换一个不够智能，每次相同
原逻辑按 `score` 降序取第一个，永远相同。

**修复：** 排序加 `±12` 随机扰动，同时 `keyword` 字段从数据库透传，让每次点换一个出现不同备选。

### 问题3：换一个后图片不更新
原 `_replaceStep` 只更新文字DOM，媒体区域停在旧图片/emoji。

**修复：**
- 提取 `_hydrateOneImage(el, name)` 独立函数供复用
- `_hydrateStepImages` 改为调用它
- `_replaceStep` 换完地点后立即重置媒体元素为新 emoji，再调 `_hydrateOneImage` 加载新图片

### 前端对接建议
- 路线现在是 AI 真正选择的，不是模板
- 换一个每次随机，不再循环同几个

---

## 2026-06-02 — 修复重复推荐同一批地点（个性化多样性）

**改动文件：** `agent/tools.py`、`agent/tool_registry.py`

### 问题
每次亲子请求必然出现太阳宫吉象亲子乐园 → METAL HANDS → 萌溪亲子营地乐园同一批地点，没有个性化多样性。

### 根因
`rank_places_for_plan` 完全确定性排序——相同的 keyword + profile 永远返回同一顺序；`session_history` 里已有上次推过的地点信息，但完全没有参与排序。

### 修复
1. **session history 降权**（`tool_registry.py`）：新增 `_extract_recently_used_places()`，从 `session_history` 的 assistant 摘要中解析最近推荐过的地点名
2. **排序降权**（`tools.py`）：`rank_places_for_plan` 新增 `recently_used_names` 参数，最近推过的地点 **-35 分**，自然沉底
3. **随机扰动**（`tools.py`）：对所有候选地点加 `±8` 随机 jitter，打破同分平局，让每次请求在相近评分的地点间有随机差异

### 测试结果
- 第一次请求 → 北京西游王国 + 奈尔宝东坝店 + 太阳宫吉象 + 星耀世界
- 第二次相同请求 → 萌溪俱乐部 + 奈尔宝荟聚店 + 北京科学中心 + 萌溪玉渊潭
- 两次结果完全不同 ✓

### 前端对接建议
- 无需前端改动，后端每次请求自动输出不同地点
- 同一用户连续规划时，已推荐过的地点会被压低排名，探索新地点

---

## 2026-06-02 — 路线步骤图片尺寸统一（4:3）

**改动文件：** `banrixian_web/css/components.css`

### 问题
路线步骤图片高度由文字内容决定（`align-self: stretch`），不同步骤图片高矮不一，视觉不和谐。

### 修复
- `.route-step__media`：移除 `align-self: stretch` 和 `min-height: 104px`，改为 `align-self: start` + `aspect-ratio: 4 / 3`（对应参考尺寸 500×375）
- 手机端同步移除 `min-height: 128px`，改用 `aspect-ratio: 4 / 3`
- `object-fit: cover` 保持不变，图片始终填满区域不变形

### 测试结果
- 桌面端、手机端图片均固定 4:3 比例，与文字卡片视觉对齐 ✓
- 无图片时 emoji 占位符同样保持 4:3 ✓

### 前端对接建议
- 图片列现在固定 4:3 比例，不受文字长短影响
- 小程序端若有图片卡片，建议统一使用 4:3（500×375）裁剪比例

---

## 2026-06-01 21:20 — 时间顺延修复 + 路线品类均衡

**改动文件：** `agent/tools.py`

### 修复1：明确时刻已过 → 自动顺延到明天

**问题：** 用户说"下午三点"但当前已是晚上 → 系统改成"现在"出发，完全不对

**根因：** `plan_time_slots` 里 `desired_start <= now` 时统一 `min_start = now+30min`，没区分"模糊时段"和"精确时刻"

**修复：**
1. `_extract_time_window`：提取到具体时刻（小时+分钟）时，打标记 `time_specified = True`
2. `plan_time_slots`：`desired_start <= now` 且 `time_specified=True` 且无明确日期（`_day_offset==0`）时：
   - 计算明天同一时刻，若仍在未来 → 顺延到明天，warning 提示"今天该时间已过，已自动顺延到明天 15:00"
   - 若连明天也不行（极端情况）→ 回退到 now+30min
3. 纯时段（"下午"、"晚上"无具体几点）仍保留原逻辑（`time_specified=False`）

**测试：** 晚上请求"下午三点出去玩" → `date=2026-06-02, start=15:00` ✓

---

### 修复2：路线品类不过密（不再出现4个餐厅）

**问题：** 路线有时出现4个餐厅/咖啡/酒吧，用户体验差

**根因：** `_fill_combo` 纯按排名填充，ranked_places 前几名全是餐饮时就全选餐饮

**修复：**
1. `_fill_combo`：餐饮类（餐厅/咖啡/酒吧）最多占 `ceil(max_len/3)` 个（3步→1, 4步→2, 5步→2）；优先选非餐饮，不够再放宽
2. `build_final_plan` 补充步骤：同样计算 `cur_food` 并与 `max_food_total` 对比，补充时优先选非餐饮

**测试：** 亲子请求 → 4个景点，无餐厅过密问题 ✓

### 前端对接建议
- 时间顺延 warning 会出现在 `time_plan.warnings` 里，前端可酌情提示用户"您要求的时间今天已过，已为您安排在明天"
- 品类均衡对所有路线风格生效（"美食"风格仍可有1-2个餐厅，但不会有4个）



## 2026-06-01 21:00 — 路线规划卡片显示真实地点图片

**改动文件：** `banrixian_web/index.html`、`banrixian_web/js/views/chat.js`、`.env`

### 问题分析
- CSS 里 `.route-step__media`（第4列图片槽）早已存在
- `_hydrateStepImages` 逻辑依赖 `source_id`（AMap POI ID），但数据库 places 表没有此字段 → 直接 return，图片永远不加载
- 尝试用 AMap REST API（静态地图）：报错 `USERKEY_PLAT_NOMATCH` —— JS API key 不能用于 REST 接口
- 正确方案：用 **AMap JS API 的 `PlaceSearch`**（同一个 key，浏览器内使用合法）按地点名称搜索、获取官方照片

### 修复内容
1. **`_hydrateStepImages` 完全重写**：
   - 用 `AMap.PlaceSearch({ extensions: "all" })` 按 `step.name` 搜索
   - 取第一个 POI 的第一张官方照片 `pois[0].photos[0].url`
   - 图片加载成功 → 替换 emoji 占位符，添加 `.has-image`
   - 图片加载失败 → 保留 emoji + 渐变占位符（静默）
2. **emoji 占位符优化**：`景点→🏛️ 餐厅→🍜 咖啡→☕ 酒吧→🍸 其他→📍`（替换原来的"地点图"文字）
3. **index.html**：预加载 `AMap.PlaceSearch` 插件
4. **.env**：写入 `AMAP_WEB_API_KEY`（备用，供后端 REST API 使用）

### 测试结果
- 知名地点（颐和园/798/北海公园）→ 显示高德官方实景照片 ✓
- 冷门或无照片地点 → 显示 emoji 渐变占位符，不报错 ✓

### 前端对接建议
- 照片来自高德 POI 搜索，有较好的覆盖率
- 如需更稳定的图片来源，可在 AMap 控制台单独申请「Web 服务」类型 key，配置 `AMAP_WEB_API_KEY` 后启用后端缓存机制



## 2026-06-01 20:45 — 高德地图 API 修复（密钥 + 安全配置）

**改动文件：** `banrixian_web/index.html`

### 问题分析
1. **错误 `<link>` 标签**：原 head 里有 `<link rel="stylesheet" href="https://webapi.amap.com/maps?v=2.0&key=...">` —— AMap API 返回的是 JS 不是 CSS，浏览器解析报错，直接删除
2. **旧 API Key**：`2d16cca3fad7586b270e6d26101ad75d` 已失效，更换为新 key
3. **缺少安全密钥**：2021年12月后申请的 key 必须配对 `securityJsCode`，且**必须在 `<script src>` 之前**注入，否则无效

### 修复内容
```html
<!-- 删除：错误的 CSS link（AMap URL 不是 CSS） -->

<!-- 新增：安全密钥注入（在 script 标签之前）-->
<script>
  window._AMapSecurityConfig = {
    securityJsCode: "bd8e9bcfb6d6b8ec5fcbe6c4074d5131"
  };
</script>

<!-- 更新：新 API Key，补充 AMap.InfoWindow 插件 -->
<script src="https://webapi.amap.com/maps?v=2.0&key=a7253de1c227d451fad7477ecda0cc84
  &plugin=AMap.Scale,AMap.ToolBar,AMap.Driving,AMap.Walking,AMap.InfoWindow"></script>
```

### 前端对接建议
- 安全密钥明文写在前端仅适合内部/开发环境；生产环境建议通过 Nginx 代理转发（服务端持有密钥）
- 地图功能（标记、驾车路线、信息窗口）修复后应全部正常加载



## 2026-06-01 20:30 — 地图真实道路路线 + 底图修复

**改动文件：** `banrixian_web/index.html`、`banrixian_web/js/views/chat.js`

### 修复：地图白色底图
- 原因：`mapStyle: "amap://styles/whitesmoke"` 是极简白色风格
- 修复：改为 `"amap://styles/normal"`（标准普通地图，显示道路/建筑/POI）

### 新增：真实驾车路线（AMap.Driving）
- 原来是直线 + 虚线，现在用 `AMap.Driving` 规划真实道路路线
- 不使用 map 参数（避免 AMap 自带样式冲突），自己提取 `routes[0].steps[].path` 坐标
- 手动绘制橙色实线（`strokeColor: #E8480A`，width 5，round 两端）
- `AMap.Driving` + `AMap.Walking` 已加入 index.html 预加载插件列表
- 支持多途经点（2-5个地点）：`driving.search(origin, dest, { waypoints }, cb)`
- 路线规划失败时自动 fallback 到品牌色虚线
- 标记点信息窗口补充了时间和简介，更清晰

**测试：** 打开地图后应显示真实道路路线（橙色实线跟着道路走），底图为普通地图 ✓

### 前端对接建议
- 地图现已显示真实路线，路线颜色与品牌色一致（#E8480A）
- Driving API 需网络请求高德路线服务，若请求失败自动降级为虚线



## 2026-06-01 20:10 — 变长行程 + 思考动画解耦 + 重复预约硬拦截

**改动文件：** `agent/tools.py`、`agent/schemas.py`、`banrixian_web/js/views/chat.js`

---

### 1. 行程步骤数随时间窗口自动变化（tools.py + schemas.py）

**问题：** 不管用户说全天还是半天，永远只有3个地点

**修复：**
- `_extract_time_window` 新增 `"全天"/"一整天"/"整天"/"全日"` → `period = "full_day"`，时间窗口 09:00-21:00
- `plan_time_slots` 新增 `full_day` 分支；时间窗口 ≥6小时时自动扩展 labels 到5项（最多5个时间槽）
- 未指定时段的"明天/后天"默认 10:00-18:00（之前是 10:00-14:00，只有4小时）
- `build_final_plan` 根据 `len(slots)` 决定 `desired_count`（2-5个）：3槽=3步，5槽=5步
- 步骤不足时从 `ranked_places` 自动补齐
- `normalize_step`（schemas.py）新增 `date` 和 `category` 字段（之前被截断！）

**测试：** "明天全天带孩子出去玩" → 5步，date=2026-06-02，时间 09:00-13:30 ✓

---

### 2. AI思考动画计时器驱动，与 SSE 解耦（chat.js）

**问题：** 思考步骤依赖 SSE `skill_call` 事件触发，到达时间不均匀，体验抖动

**修复：**
- 新增 `_startThinkSequence()` / `_tickThink()` / `_stopThinkSequence()` 计时器驱动体系
- 每步固定 550ms 依次激活（pending→active→done），与实际 SSE 事件完全解耦
- SSE `observation` 事件改为写入 `_pendingObs[skill]`，计时器到达该步时一并显示 obs 文字
- 若 obs 在计时器之后到达（step 已 done），立即更新 DOM
- SSE `done` 事件触发 `_stopThinkSequence()` 快速补全剩余步骤
- `_markAllDone()` 直接代理到 `_stopThinkSequence()`
- `skill_call` 事件不再驱动 DOM，仅更新状态文字

**效果：** 10个步骤约5.5秒匀速播完，体验流畅一致

---

### 3. 重复预约硬拦截（chat.js）

**问题：** 点击"预约此地点"后有时能重复调用接口

**修复：** `_bookStep()` 入口增加 `_bookedPlaceNames.has(name)` 检查，已预约的地点直接 `return`，不进入 API 调用路径，UI 同步更新为"✓ 已加入行程"

---

### 前端对接建议
- 步骤 JSON 新增 `date` (YYYY-MM-DD) 和 `category` 字段，小程序端可直接读取
- "全天游" 类请求将产生 4-5 个步骤；半天依然 3 个；晚上出行 2-3 个
- 含"预约/订位"的请求仍走 LLM 循环，不受快速通道影响



## 2026-06-01 19:50 — 预约状态彻底修复 + 路线生成提速 3x

**改动文件：** `banrixian_web/js/views/chat.js`、`agent/harness.py`

### 修复：预约状态无法持久化（chat.js）

**根因：** 仅靠 `plan.steps[idx].booked` 对象 mutation 不够可靠——JS 引用断链或对象重建时状态丢失

**修复方案（三重持久化）：**
1. `step.booked = true` 写入 plan 对象（原有）
2. `_bookedPlaceNames: Set<string>` — 模块级，存地点名称（不是索引），tab 切换时保持
3. `sessionStorage["brx.bookedPlaces"]` — 浏览器 session 级备份，刷新/tab 切回都生效

**渲染判断改为：**
```js
const isBooked = step.booked === true || _bookedPlaceNames.has(step.name);
```
只要地点名称被预约过，**任何情况下**都渲染为"✓ 已加入行程"（不可点击）

新规划开始 → 清空 `_bookedPlaceNames` + sessionStorage，不影响下一次规划
**测试结果：** 预约后切行程再切回来，按钮正确不可点击，无重复预约 ✓

### 优化：路线生成速度 3x 提升（harness.py）

**根因：** bootstrap（8个步骤，纯 Python，~0.5s）之后，还会进入 LLM free loop（1次 LLM API 调用，3-7s），LLM 基本只是调用 `build_final_plan`，完全可以直接在 Python 里完成

**优化方案：快速通道**
- 识别「不含预约意图」的纯规划请求（不含"预约/订位/下单/预订"等词）
- bootstrap 结束后直接调 Python `_finish_from_context()` 构建 final plan
- 发射 `skill_call_event("build_final_plan")` + `observation_event` + `done_event`
- **跳过所有 LLM free loop 调用**
- 仅含预约关键词时才走 LLM 循环

**实测结果：**
- 优化前：`~12-15s`（含 1 次 intent LLM + 1-2 次 free loop LLM）
- 优化后：**`~4-6s`**（intent LLM + 纯 Python bootstrap + `build_final_plan`）
- **提速约 3 倍**

### 前端对接建议
- 预约状态现在由 `sessionStorage["brx.bookedPlaces"]` 管理，关闭浏览器自动清除（正常行为）
- 生成速度大幅提升，前端 loading 时间同步缩短约 7-10s



## 2026-06-01 19:40 — 时间提取修复 + 路线步骤携带日期

**改动文件：** `agent/tools.py`、`routers/orders.py`、`banrixian_web/js/views/chat.js`

### 修复内容

**1. 时间提取全面升级（tools.py `_extract_time_window`）**
- 原问题：`raw` 字段在"明天下午"时写死"今天下午"，导致 intro 显示错误
- 原问题：不支持"三点"（中文数字）、"3点半"（半小时）、"14:30"（纯数字格式）
- 原问题：没有"后天"处理，只有今天/明天
- 修复：
  - `date_hint` 新增 `"day_after_tomorrow"`，"后天/大后天" 正确映射 +2天
  - `raw` 字段自动拼接日期前缀（"明天下午"、"后天上午"）
  - 时段覆盖：新增"中午"（12:00-13:30）
  - 具体时刻解析：支持"3点半"→15:30、"三点"→中文数字解析、"14:30"→纯数字HH:MM格式
  - 12/24小时自动转换（下午/晚上 + 1-11点 → 加12）
- **测试：** "明天下午带孩子去玩" → `raw="明天下午"`, `date="2026-06-02"` ✓

**2. `plan_time_slots` 逻辑修复（tools.py）**
- 原问题：说"明天"但没具体时段时，`else` 分支用 `now+45min`（今天），而非 `base`（明天）
- 修复：新增 `elif _day_offset > 0` 分支，明天/后天无时段默认10:00出发 `base.replace(hour=10)`
- 原问题：`date_hint != "tomorrow"` 判断没覆盖"后天"
- 修复：改用 `_day_offset == 0` 统一判断是否为今天

**3. 路线步骤携带 `date` 字段（tools.py）**
- `_normalize_candidate_steps`、`build_final_plan` 三处步骤构建均新增 `"date": slot.get("date")` 字段
- 步骤 date 来自 `time_plan.slots[].date`，明天的路线每个步骤 date = "2026-06-02"

**4. 预约传递正确日期（routers/orders.py + chat.js）**
- 原问题：`confirm_reservations` 硬编码 `current_date = datetime.now()`，明天的预约被记成今天
- 修复：优先使用 `item.get("date")` 字段，`desc` 显示为"2026-06-02 14:00 · 2人"
- 前端 `_renderPlan`：非今天的步骤时间显示"明天 14:00"（或"后天 ..."）
- 前端 `_bookStep`：透传 `date` 字段到预约接口

### 测试结果
- "明天下午带孩子去玩" → 所有 steps `date=2026-06-02`，`time=14:00/15:30/16:30`，UI显示"明天 14:00" ✓
- 预约明天的地点 → 订单 `date_str=2026-06-02` ✓
- "后天上午" → `date_hint=day_after_tomorrow`, date = 后天日期 ✓
- "下午三点" → `start_hint=15:00` ✓
- "3点半" → `start_hint=15:30`（下午上下文时）✓

### 前端对接建议
- 路线步骤新增 `date` 字段（"YYYY-MM-DD"格式），小程序端展示时可据此显示"明天/后天"
- 预约接口 `/v1/orders/confirm-reservations` `reservations[].date` 字段现在会被后端采纳，小程序端应透传 step.date
- 若 date 为空，后端兜底使用今天



## 2026-06-01 17:50 — 会话持久化 + 预约状态跨页面保持

**改动文件：** `banrixian_web/js/views/chat.js`

### 修复内容

**1. 刷新后恢复对话记录（chat.js）**
- 原问题：浏览器刷新（F5）后，所有对话气泡和路线消失
- 修复：`renderChat()` 首次执行时，若内存状态为空，自动从 `state.chatMessages`（已持久化到 localStorage）重建气泡列表，从 `state.lastPlan` 恢复最终路线
- 恢复时同时隐藏欢迎标题和示例提示
- **测试结果：** 刷新后对话气泡和路线正常恢复 ✓

**2. 预约状态跨页面 + 跨刷新保持（chat.js）**
- 原问题：预约某地点后切换到"行程"再回来，按钮重置为"预约此地点"，可重复预约
- 根因：之前用内存 Set `_ps.bookedSteps` 存已预约索引，但不跟路线数据绑定，路线重渲染时无法判断
- 修复：预约成功后将 `step.booked = true` 直接写入 `plan.steps[idx]`（即 `_ps.finalPlan` 的引用）；同时调用 `setLastPlan(_ps.finalPlan)` 将含 `booked` 标记的路线写入 localStorage
- `_renderPlan` 改为读 `step.booked === true` 判断按钮状态，不再依赖内存 Set
- **覆盖场景：** tab 切换、刷新、多次前后跳转，已预约地点始终显示"✓ 已加入行程"
- **测试结果：** 预约后跳行程再回来，按钮正确显示"已加入行程"，不会重复提交 ✓

### 前端对接建议
- `setLastPlan(plan)` 已在预约成功后自动调用，前端无需额外操作
- 路线数据结构新增 `steps[].booked: boolean` 字段，前端小程序若需同步预约状态可读取此字段
- 刷新恢复依赖 `state.chatMessages` / `state.lastPlan` 写入 localStorage，不影响后端



## 2026-06-01 17:31 — 前端多项修复 + 地点库扩充至180条

**改动文件：** `banrixian_web/index.html`、`banrixian_web/js/views/chat.js`、`banrixian_web/css/layout.css`、`old/place_v2.py`（新增种子脚本，已执行）

---

### 前端修复（banrixian_web）

**1. 手机端禁止缩放放大（index.html）**
- `<meta name="viewport">` 新增 `maximum-scale=1.0, user-scalable=no`
- 防止手机端双指捏合缩放，体验更接近原生 App

**2. AI思考过程切换后恢复（chat.js）**
- 原问题：规划完成后切换 Tab 再回来，思考面板消失
- 修复：`renderChat()` 的 `else if (_ps.finalPlan)` 分支新增恢复逻辑：先重建思考面板并折叠，再渲染路线结果
- **测试结果：** 切换 Tab 返回后思考面板正常恢复折叠状态 ✓

**3. 预约状态跨渲染保持（chat.js）**
- 原问题：预约成功后切换 Tab 再回来，按钮重置为"预约此地点"
- 修复：`_ps.bookedSteps: new Set()` 模块级持久化预约记录；`_renderPlan` 根据该 Set 渲染"✓ 已加入行程"或"预约此地点"；新规划时自动清空
- **测试结果：** 预约后来回切换 Tab，按钮状态正确保持 ✓

**4. 输入框固定在底部 Tab 之上（chat.js + layout.css）**
- 原问题：内容多时输入框随页面滚动，飞到上方
- 修复：输入区从 `planner-wrap` 内移出，独立为 `#chat-input-dock`，CSS `position: fixed; bottom: var(--tabs-h)`
- 桌面端自动偏移侧边栏宽度并贴底
- `force-mobile` / `force-desktop` 模式各自处理
- `.planner-wrap` 新增 `padding-bottom: 200px` 防内容被遮挡
- **测试结果：** 长内容下输入框始终固定在底部 Tab 正上方 ✓

**5. 工具调用失败不显示报错（chat.js）**
- 原问题：`observation` 事件中 `ok=false` 时显示"工具调用失败"文字
- 修复：`_buildObsText()` 检测 `data.ok === false` 时返回"已完成"，对用户隐藏底层错误
- **测试结果：** LLM 工具调用失败时思考面板显示"已完成"而非报错 ✓

---

### 后端改动

**地点库扩充（old/place_v2.py，已执行）**
- 原有 60 条 → 扩充至 **180 条**真实北京地点
- 新增类别：文化/艺术（20条）、咖啡/茶（20条）、各类别大幅补充
- 亲子：30条 | 约会：30条 | 美食：40条 | 休闲：40条 | 文化：20条（含keyword）
- 所有地点均为真实北京地点，含精确经纬度
- 已确认：`ai_routes` 仅用于 `/v1/map/top-routes` 展示接口，**规划路线100%来自 `places` 表实时推理**，绝不取 AI_ROUTES
- **测试结果：** curl 测试确认新地点（798艺术区/素然茶室/北海公园等）正常出现在规划结果中 ✓

---

### 前端对接建议
- 输入区现在是 `position:fixed`，不再在 `planner-wrap` DOM 内，前端如有手动操作输入框父节点的代码需注意
- 预约按钮状态由前端 JS `_ps.bookedSteps` Set 维护，刷新页面会清空（设计如此，刷新等于新会话）
- 手机缩放已禁用，如有需求展示地图缩放请使用地图组件内置缩放控件



## 2026-05-31 20:10 — 全面重构：响应式前端 + 后端智能化修复

**改动文件：** `routers/chat.py`、`agent/harness.py`、`banrixian_web/js/views/chat.js`、`banrixian_web/css/components.css`

### 后端修复

**1. Intent 分类大幅增强（routers/chat.py）**
- 新增 20+ 强信号词（今天/下午/出去玩/帮我/带孩子/和老婆等）
- `chat/clarify` → `plan` 自动修正更激进：含时间词 + 出行词即判 plan
- LLM prompt 重写：更清晰的触发规则，明确"今天下午出去玩"直接 plan
- **测试结果：** "今天下午出去玩" → plan ✓（之前误判为 chat/clarify）

**2. Plan intro 与 steps 地点不一致修复（agent/harness.py）**
- `_enrich_plan_steps` 新增 intro 验证：检查 intro 是否包含所有 steps 地点名
- 如不一致，自动用实际 steps 重建 intro（`已为你安排好路线：A → B → C`）
- **测试结果：** intro 与 steps 地点完全一致 ✓

### 前端重构（chat.js）

**1. 规划中切换页面再回来能恢复（核心修复）**
- 模块级 `_ps` 对象持久化规划状态（steps/thinkingContainer/finalPlan）
- `renderChat` 重渲染时检测 `_ps.active`，自动重建思考面板并更新 DOM 指针
- SSE 回调先更新 `_ps.steps`，再通过 `document.contains` 判断 DOM 是否存在

**2. 默认聊天模式（用户反馈）**
- 默认显示简洁聊天输入框（textarea + 发送按钮）
- 可点击"规划表单"切换到含位置/风格/chips 的完整表单模式
- 历史对话最多显示 8 条，包含用户/AI 双向内容

**3. 修复 onDone 数据提取 bug**
- 旧代码：`data?.plan`（永远 undefined）
- 新代码：`if (payload.type === "plan") _ps.finalPlan = payload` ✓

**4. 思考过程更详细**
- 每个步骤完成后显示 observation summary
- `_buildObsText` 从 observation data 中提取：时间/天气/地点数量/时间段/场景等
- 天气步骤显示温度和天气状况，搜索步骤显示候选数量

**5. 路线结果增强**
- 显示同行人信息（几大人+几孩子）
- 规划依据标签（thinking）默认折叠可展开
- 预约：支持勾选 pending actions 或一键全部加入行程
- 预约成功有 try/catch，错误时显示具体原因

**前端对接建议：**
- 发送消息无需额外字段，直接 `{"text": "今天下午带孩子出去玩"}`
- 规划状态跨导航保持，用户可随时切换 tab 再回来
- 预约接口 `/v1/orders/confirm-reservations` 通过验证正常

**测试结果：**
- 后端 intent: "你好"→chat ✓ | "今天下午出去玩"→plan ✓ | 带娃规划→plan ✓
- intro 与 steps 一致性验证通过 ✓
- 预约通过 nginx proxy 测试 200 OK ✓
- 服务 `pm2 status` online ✓

## 2026-05-31 17:15 — Web 前端工作台搭建

**改动目录：** `banrixian_web/`

- 新增纯静态中文 Web 前端，采用解耦结构：`index.html`、`css/`、`js/`、`js/views/`
- 实现登录/注册、API 地址配置、总览工作台、AI SSE 规划聊天、定位坐标上报、方案预览、确认预约
- 实现亲友库管理：查询、新增/更新、追加标签、删除
- 实现订单管理：订单列表、服务订单创建、删除、AI 规划预约写入订单
- 实现 Agent 记忆页：长期偏好、亲友记忆、短期会话查看与清空
- 实现个人资料页：资料读取、城市/电话/标签更新、签到入口
- 视觉上参考 Wanderlust 的编辑型 SaaS 设计语言：浅色纸感背景、细边框、强排版、路线票据和地图式信息面板，但未复制其品牌内容

**测试结果：**
- `python -m compileall -q main.py core routers agent skills` 通过
- `node --check banrixian_web/js/*.js banrixian_web/js/views/*.js` 通过
- `curl -I http://127.0.0.1:5173/` 返回 200
- `curl -I http://127.0.0.1:3000/docs` 返回 200
- 使用现有测试 token 调用 `/v1/user/profile` 返回 200，资料接口可用

**前端对接建议：**
- 当前 Web 默认 API 地址为 `http://127.0.0.1:3000`，可在左下角输入框切换服务器地址
- `/v1/chat/send` 前端已按 SSE 流式解析 `status/intent/skill_call/observation/done/error`
- AI 规划产生的 `steps` 会用于方案预览，点击“确认预约”会调用 `/v1/orders/confirm-reservations`

## 2026-05-31 17:18 — Web 前端公网访问配置

**改动文件：** `banrixian_web/js/config.js`

- 将前端默认 API 地址从本机 `http://127.0.0.1:3000` 调整为公网后端 `http://120.46.81.119:3000`
- 使用 PM2 启动前端静态服务 `banrixian-web`，监听 `0.0.0.0:5173`

**测试结果：**
- `pm2 restart banrixian-web` 后服务 online
- `curl -I http://127.0.0.1:5173/` 返回 200
- `node --check banrixian_web/js/config.js` 通过

**前端对接建议：**
- 公网访问地址为 `http://120.46.81.119:5173/`
- 如需不带端口访问 `http://120.46.81.119/`，需要额外配置 Nginx 或将 80 端口转发到 5173

## 2026-05-31 17:22 — Nginx 单端口公网入口

**改动文件：** `banrixian_web/js/config.js`、`nginx/banrixian-web.conf`、`nginx_runtime/nginx.conf`

- 前端默认 API 地址调整为同源 `/api`
- 新增 Nginx 配置：监听 `8080`，静态文件服务指向 `banrixian_web`
- API 统一通过 `/api/` 转发到本机后端 `http://127.0.0.1:3000/`
- 使用 PM2 托管用户态 Nginx：服务名 `banrixian-nginx`
- 停用旧的 `banrixian-web` 端口 5173 静态服务，避免多个公网入口混淆

**测试结果：**
- `nginx -t -p /home/bairixian/bairixian_AI/nginx_runtime -c /home/bairixian/bairixian_AI/nginx_runtime/nginx.conf` 通过
- `pm2 start ... --name banrixian-nginx` 后服务 online
- `curl -I http://127.0.0.1:8080/` 返回 200
- `curl http://127.0.0.1:8080/api/v1/user/profile` 经 Nginx 代理后返回 200
- `pm2 save` 成功保存进程列表

**前端对接建议：**
- 服务器后台只需要放行公网端口 `8080`
- 公网访问页面：`http://120.46.81.119:8080/`
- 前端接口统一走同源 `/api/v1/...`，不要再直接调用公网 `:3000`

## 2026-05-31 17:25 — 系统 Nginx 80 端口入口

**改动文件：** `nginx/banrixian-web.conf`

- 将 Nginx 正式入口调整为 `80` 端口，目标访问地址为 `http://120.46.81.119/`
- 将前端静态文件发布到 `/var/www/banrixian_web`，避免系统 Nginx 访问用户 home 目录权限不足
- 禁用 Nginx 默认站点，启用 `banrixian-web.conf`
- 保持 API 隔离：前端访问 `/api/v1/...`，Nginx 内部代理到 `http://127.0.0.1:3000/v1/...`
- 停用旧的 PM2 用户态 Nginx `banrixian-nginx`，公网入口不再使用 `8080`

**测试结果：**
- `nginx -t` 通过
- `systemctl reload nginx` 成功
- `curl -I http://127.0.0.1/` 返回 200
- `curl http://127.0.0.1/api/v1/user/profile` 返回 200
- `curl --noproxy '*' -I http://120.46.81.119/` 返回 200

**前端对接建议：**
- 云服务器后台只需要放行公网端口 `80`
- 用户访问 `http://120.46.81.119/` 即可进入界面
- 接口仍然统一走同源 `/api/v1/...`，不要暴露 `3000` 给前端用户

## 2026-05-31 18:04 — AI 规划页体验简化

**改动文件：** `banrixian_web/index.html`、`banrixian_web/js/state.js`、`banrixian_web/js/main.js`、`banrixian_web/js/views/chat.js`、`banrixian_web/css/components.css`

- 默认进入“AI 规划”页，不再先进入复杂总览
- 将“API”文案改为“服务连接（一般不用改）”，默认使用同源 `/api`
- 自动迁移旧的 `127.0.0.1:3000` / `120.46.81.119:3000` 连接配置，避免浏览器缓存旧地址导致接口异常
- AI 规划对话和上一次路线保存到本地，切换页面再回来不会丢失
- 新增“清空”按钮，用户可主动清除当前对话
- 流式状态文案从“工具调用/observation”改为“读取地点库、查找真实地点、给地点排序、整理最终路线”等可理解表达
- 规划结果在 `observation` 阶段拿到完整 plan 时立即显示，不必等最后 `done` 事件，减少“规划一直不显示”的体感
- 路线结果区强化展示：风险标签、时间线、地点原因、安排理由

**测试结果：**
- `node --check banrixian_web/js/*.js banrixian_web/js/views/*.js` 通过
- 通过 Nginx `/api/v1/chat/send` 实测可收到完整 SSE 流和最终 `done` 规划结果
- 已发布新版前端到 `/var/www/banrixian_web` 并重载 Nginx

**前端对接建议：**
- 用户入口继续保持 `http://120.46.81.119/`
- 前端本地缓存了对话，如遇旧页面缓存，可让用户强刷浏览器或点击“清空”

## 2026-05-31 18:10 — AI 规划页产品化重构

**改动文件：** `banrixian_web/js/views/chat.js`、`banrixian_web/css/components.css`、`banrixian_web/css/layout.css`、`banrixian_web/index.html`

- 将 AI 规划页从“后台聊天界面”重构为“行程规划产品界面”
- 首页文案改为价值表达：强调 30 秒生成顺路行程、避开排队和绕路
- 增加示例卡片，点击后直接生成路线，降低冷启动门槛
- 增加示例结果卡片，让用户未输入前也知道最终会得到什么
- 增加右侧路线地图，以 SVG 示意真实点位顺序，生成后自动用路线经纬度绘制
- 增加实时规划状态：理解需求、判断天气、搜索地点、计算距离、优化顺序、生成行程
- 将输入区升级为规划表单：位置/商圈、路线风格、少走路/避开排队/适合带娃/下雨也能玩快捷标签
- 明确定位限制：当前 HTTP 访问不能稳定自动定位，改为手动输入区县/商圈；后续 HTTPS 后再开启一键定位
- 导航简化为：AI 规划、历史路线、个人中心，减少首次使用干扰
- 预约按钮只在生成路线后出现，避免“还没路线就预约”的疑惑

**测试结果：**
- `node --check banrixian_web/js/*.js banrixian_web/js/views/*.js` 通过
- 新版前端已发布到 `/var/www/banrixian_web`
- `curl --noproxy '*' -I http://120.46.81.119/` 返回 200
- `/api/v1/user/profile` 经 Nginx 代理返回 200

**前端对接建议：**
- 继续使用 `http://120.46.81.119/` 作为唯一用户入口
- 当前位置先用“位置/商圈”输入框承接，等域名和 HTTPS 配好后再接浏览器定位或地图 SDK

## 2026-05-31 18:15 — TripIt 风格与真实地图接入

**改动文件：** `banrixian_web/index.html`、`banrixian_web/js/views/chat.js`、`banrixian_web/css/base.css`、`banrixian_web/css/components.css`、`banrixian_web/css/layout.css`

- 参考 TripIt 的行程组织思路：蓝白配色、清晰 itinerary 卡片、强 CTA、可信提醒和地图并列展示
- 接入 Leaflet 真实地图组件，不再使用 SVG 假地图
- 地图瓦片使用 OpenStreetMap，生成路线后根据后端返回的真实 `lat/lng` 添加 marker 和 polyline
- 未生成路线前展示北京示例点位，避免右侧空白
- 生成路线后地图自动 fitBounds 到路线点位范围
- 首页/规划页视觉从“纸感后台”调整为更干净的旅行产品风格

**测试结果：**
- `node --check banrixian_web/js/*.js banrixian_web/js/views/*.js` 通过
- 新版前端已发布到 `/var/www/banrixian_web` 并重载 Nginx
- `curl --noproxy '*' -I http://120.46.81.119/` 返回 200
- `/api/v1/user/profile` 经 Nginx 代理返回 200
- 已确认发布目录包含 Leaflet / OpenStreetMap 相关代码

**前端对接建议：**
- 用户访问入口不变：`http://120.46.81.119/`
- 地图依赖浏览器访问 `unpkg.com` 和 OpenStreetMap 瓦片服务；如果后续要国内稳定访问，建议替换为高德/腾讯地图 Web SDK

## 2026-05-31 16:00 — 定位功能接入

**改动文件：** `routers/chat.py`、`agent/harness.py`、`skills/weather.py`（已有）

- `/v1/chat/send` 新增接收 `lat`/`lng`/`district` 字段，构建 `enriched_user` 注入定位数据
- 天气查询优先使用真实 GPS 坐标（而非城市名查表），精度更高
- Agent 规划路线时通过 Haversine 公式计算用户到每个地点的真实距离，并据此评分排序
- 前端传坐标示例：`{"text":"...", "lat":39.921, "lng":116.446, "district":"朝阳区"}`

**测试结果：** 传入朝阳区坐标后，天气 API 使用真实坐标，距离评分正常（3km内=18分），reason 显示"距离你当前位置较近" ✓

---

## 2026-05-31 16:50 — 亲友管理功能完善

**改动文件：** `routers/user.py`、`agent/memory_store.py`

### 1. 亲友 CRUD 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/v1/user/family` | 查询亲友列表 |
| POST | `/v1/user/family` | 新增亲友 `{relation, avatar, tags}` |
| PUT | `/v1/user/family/{id}` | 全量更新亲友信息 |
| PATCH | `/v1/user/family/{id}/tags` | 追加 tags（不覆盖已有） |
| DELETE | `/v1/user/family/{id}` | 删除亲友 |

同一用户同一 `relation` 只保留一条记录（POST 时自动 upsert）。

### 2. 对话自动提取亲友 + 细粒度特征

`memory_store.py` 新增 `_auto_upsert_family_members()`，每次对话完成后自动触发：

- 支持识别的关系：老婆/老公/女朋友/男朋友/儿子/女儿/孩子/爸爸/妈妈/父母/爷爷/奶奶/朋友（共13种）
- 自动分配 emoji avatar
- 按**句子分句**归属特征，防止跨句 tag 混淆
- 可提取 30+ 种特征 tag，包括：
  - 饮食：减肥中、清淡、忌口香菜、不吃辣、素食、控糖等
  - 健康：腿脚不便、高血压、心脏病、孕妇、晕车
  - 学段：上幼儿园、上小学、上初中、上高中、上大学
  - 年龄：X岁（从文本提取数字，仅孩子类关系）
  - 兴趣：爱拍照出片、爱逛街、爱美食、爱运动、爱艺术文化、爱自然户外
  - 性格：怕热、怕冷、内向安静等
- 新成员自动写入 `family_members` 表（有 tag 才建，避免刷空记录）
- 已有成员追加新 tag，上限 20 条，不覆盖已有标签

**测试结果：**
- 发送"我女儿刚上幼儿园，特别爱拍照，我老婆在减肥不吃辣"
- 自动新建：女儿（上幼儿园、爱拍照出片）✓，老婆（减肥中、不吃辣、爱拍照出片）✓
- 手动 CRUD 接口全部正常 ✓
